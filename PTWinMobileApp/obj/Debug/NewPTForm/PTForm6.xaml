<Page
    x:Class="PTWinMobileApp.PTForm6"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PTWinMobileApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <TextBlock FontSize="20" Margin="10,32,142,549" >
        	<Run Text="What makes your condition "/>
        	<LineBreak/>
        	<Run Text="worse(check all that apply):"/>
        </TextBlock>
        <CheckBox x:Name="cb_bending" Content="Bending" Margin="10,88,0,0" Height="64" />
        <CheckBox x:Name="cb_sitting" Content="Sitting" Margin="10,133,0,0" />
        <CheckBox x:Name="cb_rising" Content="Rising" Margin="10,178,0,0" />
        <CheckBox x:Name="cb_prologned_position" Content="Prologned Positioning" Margin="10,352,0,0" />
        <CheckBox x:Name="cb_movement" Content="Movement" Margin="10,222,0,0" />
        <CheckBox x:Name="cb_standing" Content="Standing" Margin="222,178,0,0" />
        <CheckBox x:Name="cb_walking" Content="Walking" Margin="222,133,0,0" />
        <CheckBox x:Name="cb_lying" Content="Lying" Margin="222,88,0,0" />
        <CheckBox x:Name="cb_rest" Content="Rest" Margin="222,307,0,0" />
        <CheckBox x:Name="cb_cough" Content="Cough" Margin="10,307,0,0" Width="123" />
        <CheckBox x:Name="cb_sneeze" Content="Sneeze" Margin="222,263,0,0" />
        <CheckBox x:Name="cb_breath" Content="Deep Breath" Margin="222,222,0,0" />
        <CheckBox x:Name="cb_medication" Content="Medication" Margin="10,263,0,0" />
        <Button x:ConnectionId='1' x:Name="bt_nextstep" Content="Next Step"                         Margin="281,563,0,20" />

    </Grid>
</Page>

